model Gratuity {
  emp_id           Int      @id @unique
  gratuity_amount  Decimal  @db.Decimal(10, 2)
  application_id   Int?
  date_of_relieving DateTime

  // Add any additional fields or relationships here
}
